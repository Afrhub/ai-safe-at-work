// Copy to config.js (gitignored) and fill from your Supabase project settings → API.
// The anon key is safe in the browser — RLS protects all data. NEVER put the service-role key here.
window.AISW_CONFIG = {
  url:  "https://YOUR-PROJECT-ref.supabase.co",
  anon: "YOUR-ANON-PUBLIC-KEY",
};
